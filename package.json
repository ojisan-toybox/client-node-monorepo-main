{
  "name": "client-node-monorepo-main",
  "packageManager": "yarn@3.5.1",
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.1",
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "_server": "yarn workspace server ",
    "server:build": "yarn run _server build",
    "server:dev": "yarn run _server dev",
    "server:start": "yarn run _server start",
    "client": "yarn workspace client",
    "client:build": "yarn run client build",
    "client:dev": "yarn run client dev"
  }
}
